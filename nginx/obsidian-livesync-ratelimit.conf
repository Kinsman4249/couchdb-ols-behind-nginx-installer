# limit_req_zone must live in the http context; conf.d/*.conf is included there.
# Generous by design: LiveSync bursts during replication. This is only an
# anti-abuse net; a host firewall and/or fail2ban do the real work.
limit_req_zone $binary_remote_addr zone=ols_couch:10m rate=30r/s;
